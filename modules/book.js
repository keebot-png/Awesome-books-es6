// Book class: to represent a book
class Book {
    constructor(title, author) {
      this.title = title;
      this.author = author;
    }
  }

const newerBook = new Book();

export default newerBook;
